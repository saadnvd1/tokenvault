# ideas

- Rotate or cap `audit.log` (it grows forever; `tv audit check` only scans one window before its cursor, but `tv audit` reads the whole file).
- Per-session burst thresholds: a burst from an unattended Claude session matters more than from Saad's shell.
- Flag reads from an unexpected cwd (e.g. a repo that has never read that entry before).
- Log `tv list` too, if name enumeration turns out to matter.
